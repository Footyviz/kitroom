---
'@footyviz/locker-room': minor
---

Add `<fv-match-card>` formation — composes `<fv-team>`, `<fv-stat-row>`, `<fv-chip>`, and other primitives into a match summary card. Supports CSS subgrid alignment via `.fv-match-card-grid` wrapper.
