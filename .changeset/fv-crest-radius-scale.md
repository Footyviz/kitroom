---
"@footyviz/kitroom": patch
---

Scale `<fv-crest>` border-radius with size: 20px crest now uses --radius-sm (4px), 28px and 36px crests use --radius-md (8px). Previously all three sizes used --radius-sm. No token additions; existing radius scale unchanged.
